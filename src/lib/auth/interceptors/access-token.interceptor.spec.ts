import { AccessTokenInterceptor } from "./access-token.interceptor";

describe("AccessTokenInterceptor", () => {
  it("should create an instance", () => {
    expect(new AccessTokenInterceptor()).toBeTruthy();
  });
});
