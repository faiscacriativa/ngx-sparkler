import { RefreshTokenInterceptor } from "./refresh-token.interceptor";

describe("RefreshTokenInterceptor", () => {
  it("should create an instance", () => {
    expect(new RefreshTokenInterceptor()).toBeTruthy();
  });
});
